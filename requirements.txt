# Flask and API
flask==3.0.0
flask-restful==0.3.10
flasgger==0.9.7.1
gunicorn>=21.2.0

# Database
psycopg2-binary==2.9.9

# Environment variables
python-dotenv==1.0.0

# Hugging Face and ML
# Using torch 2.9.1 with CUDA 12.6 (matches local machine version)
torch==2.9.1
torchvision==0.24.1
transformers>=4.30.0
accelerate>=0.20.0
huggingface-hub>=0.16.0
peft>=0.3.0
safetensors>=0.3.0
sentencepiece>=0.1.99
protobuf>=6.33.1

# OpenAI (for guardrails)
openai>=1.0.0
tiktoken>=0.5.0

# Utilities
numpy>=1.24.0,<2.0.0  # numpy 2.x has breaking changes, keep <2.0 for compatibility
requests>=2.31.0
pyyaml>=6.0
Pillow>=10.0.0

# LangChain (if needed)
langchain-classic>=0.1.0

# Langfuse for LLM observability and tracing
langfuse>=2.0.0

